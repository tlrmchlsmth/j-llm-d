# 24 GPU disaggregated: 2 prefill (TP=4) + 2 decode (TP=8)
# Base: glm-disagg
expert_parallel: false
decode:
  parallelism:
    tensor: 8
  replicas: 2

prefill:
  parallelism:
    tensor: 4
  replicas: 2
