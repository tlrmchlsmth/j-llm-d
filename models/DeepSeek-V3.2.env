# Prefill topology for deepseek-ai/DeepSeek-V3.2
# Does not fit in 4 GPUs — use multi-node LWS group with DP across pods
PREFILL_REPLICAS=1
PREFILL_LWS_SIZE=2
PREFILL_TP_SIZE=4
DECODE_GPU_MEMORY_UTILIZATION=0.75
PREFILL_GPU_MEMORY_UTILIZATION=0.85
